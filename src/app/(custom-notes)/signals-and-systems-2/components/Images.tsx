export function FP({ name }: { name: string }) {
    return <img src={`/custom-media/probability/fp/${name}.jpg`} alt="" />;
}

export function Notes({ name }: { name: string }) {
    return <img src={`/custom-media/probability/notes/${name}.jpg`} alt="" />;
}

